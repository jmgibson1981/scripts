Simple cups server. Execute as follows

docker run -id -h "$NAME" --name "$NAME" --network host --mount source="$NAME",destination=/var/cache/cups --restart always "$NAME"

Access at dockerhost:631

Default login = printers / printers
